//Tu tem que estar com um box collider(sem ser 2D, acredito) tanto no prefab do machado, quanto no inimigo, e no inimigo tu tbm vai por o Rigidbody.

ShootCollision : MonoBehaviour

private void OnCollisionEnter(Collision collision)
{
  Destroy(this.gameObject);
}
